def mystery(n: int) -> int:
    """Compute the square of n minus 1.

    Args:
        n (int): Givent integer, where 0 <= n <= 10^3.

    Returns:
        int: The result of n squared minus 1.
    """
    return n**2 - 1
